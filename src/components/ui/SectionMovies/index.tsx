import { ChildrenParams } from '../../../core/ChildrenParams'
import { SectionStyle } from './styles'

export function SectionMovies({ children }: ChildrenParams) {
  return <SectionStyle>{children}</SectionStyle>
}
